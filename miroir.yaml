# Test config for observability verification
shards: 64
replication_factor: 1
replica_groups: 1
task_store:
  backend: sqlite
  path: /tmp/miroir-tasks-test.db
server:
  bind: "127.0.0.1"
  port: 7700
search_ui:
  enabled: false
admin_ui:
  enabled: false
